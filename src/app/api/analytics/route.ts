import { NextRequest, NextResponse } from "next/server";
export async function POST(request:NextRequest) { try { const body=await request.json(); if(typeof body.type !== "string") return NextResponse.json({error:"Invalid event"},{status:400}); if(process.env.DATABASE_URL) { const { PrismaClient } = await import("@prisma/client"); const globalForPrisma=globalThis as unknown as { prisma?: InstanceType<typeof PrismaClient> }; const prisma=globalForPrisma.prisma ?? new PrismaClient(); const payload=body.payload ?? {}; const source=["douyin","wechat","friend","share","direct"].includes(payload.source) ? payload.source : "direct"; if(process.env.NODE_ENV !== "production") globalForPrisma.prisma=prisma; await prisma.userEvent.create({data:{eventType:body.type,sessionId:typeof body.sessionId === "string"?body.sessionId:null,source,agentResult:typeof payload.agent === "string"?payload.agent:null,payload}}); } return NextResponse.json({ok:true}); } catch { return NextResponse.json({ok:true}); } }
