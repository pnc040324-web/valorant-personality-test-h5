# 特工头像资源目录

将已授权、已压缩的 Q 版特工头像放入本目录，文件名与 src/data/agents.json 的 id 保持一致。

示例：omen.webp、sage.webp、jett.webp、cypher.webp。

推荐使用正方形 WebP（512 × 512 或更小）。未提供某个文件时，客户端会自动使用 /images/agent-placeholder.svg，不会展示破图。
